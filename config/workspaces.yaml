# Workspace Configuration
# 使用相对路径，便于项目迁移

# Workspace List
# 使用相对于 ai-driven 根目录的路径

workspaces:
  # - name: example_space
  #   path: workspaces/example_space
  #   code_roots:
  #     - ../york/example-repo    # 相对于 workspace 目录
