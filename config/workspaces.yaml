# Workspace Configuration
# 使用相对路径，便于项目迁移

# Workspace List
# 使用相对于 ai-driven 根目录的路径

workspaces:
  - name: test_workspace
    path: workspaces/test_workspace
    code_roots:
      - ../ai-projects/test_project
