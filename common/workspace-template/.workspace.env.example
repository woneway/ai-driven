# AI-Driven Workspace 配置
# workspace 级别配置会覆盖全局设置
# 路径都是相对于 ai-driven 根目录

# ========== Workspace 信息 ==========
# 此部分由 init-space.sh 自动生成

# SPACE_NAME=my_workspace

# ========== Hooks 配置 ==========

# 是否启用 hooks（true/false），覆盖全局设置
# HOOK_ENABLED=true

# 各 hook 开关（默认继承全局设置，设为 false 禁用）
# HOOK_SESSION_START=true
# HOOK_SESSION_END=true
# HOOK_SUBAGENT_START=true
# HOOK_SUBAGENT_STOP=true
# HOOK_BEFORE_SHELL=true
# HOOK_AFTER_SHELL=true
# HOOK_POST_TOOL=true

# ========== 通知配置（可选） ==========

# 启用哪些通知渠道（逗号分隔）：wechat,dingtalk,sms
# NOTIFY_ENABLED_CHANNELS=dingtalk

# 钉钉 webhook
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
# DINGTALK_KEYWORD=cursor

# 通知的最低耗时阈值（秒），超过此时间才发送通知
# NOTIFY_MIN_DURATION=0
